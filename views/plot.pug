extends layout

block content
	.py-4.text-center
		h2 Espaço Exploratório
		p.lead
			| Aqui você tem a liberdade para fazer a sua pesquisa exploratória
			| sobre a projeção do cenário eleitoral nas mídias digitais

	block plotArea
		.row.justify-content-around
			.col-8.order-md-2.order-2.bg-light
				img(src="/imagens/Gráfico_exemplo.png", alt="Gráfico requisitado", width="100%")
			.py-2.col-md-3.order-md-1.order-1.bg-light
				.py-2.h4.mb-3.text-center Controle
				form.needs-validation(novalidate='')
					.row
						.py-2.col-md-12
							+digitalMedia
						.py-2.col-md-12
							+queries
						.py-2.col-md-12
							+categories
						.py-2.col-md-12
							label(for="actors") Atores:
					.py-2.mb-4
						+actors
					.d-block.my-3
						.custom-control.custom-checkbox
							input#clearActors.custom-control-input(type="checkbox")
							label.custom-control-label(for="clearActors") Limpar atores

mixin digitalMedia
	label(for="digitalMedia") Mídia digital:
		select#digitalMedia.custom-select.d-block.w-100(required='')
			option(value='') Escolha...
			option Facebook
			option Instagram
			option Twitter
			option Youtube
		.invalid-feedback
			| Por favor selecione uma mídia válida.

mixin queries
	label(for="query") Aspecto de análise:
		select#query.custom-select.d-block.w-100(required='')
			option(value='') Escolha...
			option Curtidas
			option Seguidores
		.invalid-feedback
			| Por favor selecione um aspecto válido.

mixin categories
	label(for="category") Categorias:
		select#category.custom-select.d-block.w-100(required='')
			option(value='') Escolha...
			option Frentes/Coletivos
			option Organizações da Sociedade Civil
			option Candidatos à presidência
			option Todos
		.invalid-feedback
			| Por favor selecione um aspecto válido.

mixin actors
	-for(let cnt = 1; cnt < 10; cnt++)
		-let _id = "id" + cnt
			.custom-control.custom-checkbox
				input.custom-control-input(type="checkbox", id=_id)
				label.custom-control-label(for=_id) Ator #{cnt}
